const perfectScore = 100;

export function func_sum(num1, num2){
    return num1 + num2;
}

export const sum = (num1, num2)  => {
    return num1 + num2;
}

export const avg = (num1, num2) => {
    return (num1 + num2)/2
}

//변수, 함수, class 앞에 export를 붙임으로써 외부에서 사용할 수 있게 된다.